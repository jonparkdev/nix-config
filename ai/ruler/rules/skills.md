# Skills

All Claude Code skills are managed in `~/nix-config/ai/skills/`. Do not create skills in `.claude/skills/` or `.claude/commands/`.

When creating a new skill, use the `/create-agent-skill` command for best-practice guidance on structure, frontmatter, and progressive disclosure. Place the result at `~/nix-config/ai/skills/<skill-name>/SKILL.md`.
